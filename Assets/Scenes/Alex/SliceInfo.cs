using UnityEngine;

//attached to sliced mesh pieces to track position in slice tree

public class SliceInfo : MonoBehaviour
{
    public SliceOperation sliceOperation;
    public bool isUpperHull;
}
